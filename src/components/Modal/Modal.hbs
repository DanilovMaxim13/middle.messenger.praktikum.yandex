<div class="modal {{className}}">
	<div class="modal__body">
		<p class="modal__title">{{title}}</p>
		{{{ Input }}}
		<div class="modal__buttons">
			{{{ ButtonSubmit }}}
			{{{ ButtonClose }}}
		</div>
	</div>
</div>
