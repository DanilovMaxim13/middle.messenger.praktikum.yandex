.button {
    width: 100%;
    max-width: 380px;

    padding: 12px;

    border: none;
    border-radius: 8px;

    font-size: 13px;

    background: var(--secondaryColor);
    color: var(--primaryBackgroundColor);
    cursor: pointer;
}