.tools {
	background-color: white;
	border: 2px solid var(--gray-color);
	border-radius: 15px;
	display: flex;
	flex-direction: column;
	gap: 15px;
	padding: 20px;
	position: relative;
	right: -20px;
	top: 100px;

	&_hidden {
		display: none;
	}
}
