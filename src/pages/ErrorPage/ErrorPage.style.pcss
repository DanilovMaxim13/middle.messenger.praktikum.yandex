.error-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 25px;

    &__error-code {
        font-size: 26px;
        font-weight: bold;
    }

    &__error-description {
        font-size: 18px;
    }
}
